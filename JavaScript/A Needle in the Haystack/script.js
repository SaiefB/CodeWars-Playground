function findNeedle(haystack) {
  // Loop through array
  // for each array item check if the item is equal to "needle"
  // if found return "found the needle at position _"
  // if not found return "your function didn't return anything"
}
